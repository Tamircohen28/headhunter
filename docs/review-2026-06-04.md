# Documentation review — 2026-06-04

HeadHunter (`job4u`) sweep. Scope: `README.md`, `docs/**` (production-master-specific axes adapted).

## Axis 0 — Repo inventory

| Entity | Count | Notes |
|--------|------:|-------|
| Subagents (`agents/*.md`) | 14 | Includes `topic-researcher` (legacy; Deep Research preferred) |
| Skills (`skills/*/SKILL.md`) | 16 | README listed 15 before sweep |
| Commands (`commands/*.md`) | 26 | |
| Scripts (`scripts/*.js`) | 29 | |

**Consistency targets:** `AGENTS.md` (claims 21 test checks ✓), no `CLAUDE.md` in this repo.

## Per-file audit

| File | Verdict | Fixes |
|------|---------|-------|
| [README.md](../README.md) | review → fixed | Test count 17→21; research lifecycle wording; `pipeline-output` blurb; `OPENAI_*` env vars; added `scaffold-base44-app` skill |
| [ARCHITECTURE.md](./ARCHITECTURE.md) | review → fixed | Broken link to Base44 skill (`../skills/…`); subagents row; test count 17→21; references tree |

## Axis 3 — Plan files

- `task.md` (repo root) — internal backlog, not in `docs/`; left untracked in git if present.
- No stray plan files under `docs/`.

## Axis 6 — Cross-doc consistency (out of scope but patched)

- [commands/research.md](../commands/research.md) — updated stage list to match Deep Research layout (was still `topic-researcher` fan-out).

## Link validation

- **Fixed:** `docs/ARCHITECTURE.md` → `skills/scaffold-base44-app/SKILL.md` (missing `../` prefix).
- **Warnings (kept):** Documentation table uses path-style link text in README — acceptable for a reference index.

## Remaining drift (not in `docs/**` scope)

| File | Issue |
|------|--------|
| `references/pipeline-output.md` | Documents deprecated `data/pipelines/` — intentional |
| `agents/topic-researcher.md` | Still present for legacy; skill says use Deep Research |
| `task.md` | Old fan-out / ultracode notes |

## Summary

**Reviewed 2/2 in-scope files. 9 fixes applied. 0 files untracked.**

Re-run link check after fixes:

```bash
bash ~/.cursor/skills/docs-review/scripts/validate-links.sh docs/ARCHITECTURE.md
```
